title: |
  Client secret [${clientSecret.secretName}] will expire
message: |
  The client secret ${clientSecret.secretName} for the ${resourceType} ${application.name} from domain ${domain.name}
  will expire at ${clientSecret.expiresAt?datetime}.