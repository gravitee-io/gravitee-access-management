{
  "type" : "object",
  "id" : "urn:jsonschema:io:gravitee:am:botdetection:google:recaptcha:GoogleReCaptchaV3Configuration",
  "properties" : {
    "detectionType": {
      "title": "Detection Type",
      "description": "Type of bot detection",
      "default": "CAPTCHA",
      "readOnly": true
    },
    "siteKey": {
      "title": "Site Key",
      "type" : "string",
      "description": "Site Key provided by the Google"
    },
    "secretKey": {
      "title": "Secret Key",
      "type" : "string",
      "description": "Secret Key link to the Site Key"
    },
    "serviceUrl": {
      "title": "Service Url",
      "type" : "string",
      "default": "https://www.google.com/recaptcha/api/siteverify",
      "description": "URL to contact to validate the CAPTCHA token"
    },
    "tokenParameterName": {
      "title": "Parameter Name",
      "description": "Parameter name used by the UI to send the token generated by Google reCAPTCHA",
      "type" : "string",
      "default": "X-Recaptcha-Token"
    },
    "minScore": {
      "title": "Minimum Score",
      "description": "Minimum value the reCAPTCHA v3 service must return to trust the request",
      "type" : "number",
      "default": 0.5,
      "minimum": 0
    }
  },
  "required": ["siteKey", "secretKey", "serviceUrl", "tokenParameterName"]
}