log_level = "INFO"
domains = ["spire-oidc", "localhost"]

# Plain HTTP for local development only. Production must serve over TLS.
insecure_addr = ":8443"

workload_api {
    socket_path = "/run/spire-agent/public/api.sock"
    trust_domain = "am.local"
}
