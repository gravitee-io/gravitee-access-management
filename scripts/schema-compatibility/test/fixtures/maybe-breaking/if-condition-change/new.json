{
  "type": "object",
  "properties": { "protocol": { "type": "string" }, "cert": { "type": "string" } },
  "if": { "properties": { "protocol": { "const": "tls" } } },
  "then": { "required": ["cert"] }
}
