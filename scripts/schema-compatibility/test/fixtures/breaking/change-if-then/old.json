{
  "type": "object",
  "properties": {
    "protocol": { "type": "string" },
    "certificate": { "type": "string" }
  },
  "if": { "properties": { "protocol": { "const": "https" } } },
  "then": { "required": ["certificate"] }
}
